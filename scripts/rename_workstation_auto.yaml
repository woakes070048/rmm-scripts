name: rename_workstation_auto.ps1
description: Rename a Windows device and sync the same name to SuperOps
language: PowerShell
runAs: SYSTEM_USER
timeout: 15
tags:
  - Administration
  - Windows
shared: true
favourite: false
readme: |
  Purpose
  -------
  Rename a Windows device and sync the same name to SuperOps.

  Naming pattern (max 15 chars): CLIENT3-USERUUID
  - CLIENT3: 3-char abbreviation of client name (always forced)
  - USER: Sanitized username, maximized, truncated if needed
  - UUID: SMBIOS UUID tail, at least 3 chars

  Usage Notes
  -----------
  - Requires SuperOps runtime variables:
    - $YourApiKeyHere: SuperOps API key
    - $YourAssetIdHere: Asset ID (placeholder)
    - $YourAssetNameHere: Current asset name (placeholder)
    - $YourClientNameHere: Client name (placeholder)
  - Only A-Z, 0-9, and hyphen used in hostname
  - Always exactly 15 characters
  - SuperOps asset name updated via GraphQL API
  - Reboot required for hostname change to take effect

  Requirements
  ------------
  - Windows 10/11
  - Administrator privileges
  - SuperOps agent installed
  - Internet access for SuperOps API
runtimeVariables:
  - name: YourApiKeyHere
placeholderVariables:
  - name: YourAssetIdHere
    label: Asset ID
    column: assetId
  - name: YourAssetNameHere
    label: Asset name
    column: name
  - name: YourClientNameHere
    label: Client name
    column: client.name
scriptId: "8083616885623574528"
lastPushedChecksum: sha256:66097951d0d86e7e
lastPushedMetaChecksum: sha256:c8a39bf8c5e0d921
